/**
 * 
 * @param {number[][]} intv 
 * @param {(number[]) => number} fn 
 */
export function reduce2d(intv, fn) {
  return intv.reduce((acc, minmax) => {
    
  }, 0)
}

reduce2d([[0,1],[0,1]], (acc, coordinates) => {
  return 0;
})

